Agent: dataset

Responsibility: Own dataset schemas, fixtures, and dataset generation scripts.

Allowed changes: files within agents/dataset/** and additive changes to datasets/** only via PR.

Outputs: schema YAMLs, example fixtures, validators, and tests.
